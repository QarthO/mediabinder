services:
  web:
    build:
      target: development
    environment:
      NODE_ENV: development
    volumes:
      - ./src:/app/src
      - ./public:/app/public
      - ./scripts:/app/scripts
      - ./migrations:/app/migrations
      - ./vite.config.ts:/app/vite.config.ts
